# Install PhatomJS and register it in PATH

How to [quick start with PhantomJS](https://phantomjs.org/quick-start.html).

## Run script:
phantomjs --ssl-protocol=any --ignore-ssl-errors=yes ./src/index.js
